package controllers;

import models.Result;

import java.util.Scanner;

public class LoginMenuController {
    public static Result login(String userName, String password,String stayLoggedIn);
    public static Result forgetPassword(String username, Scanner scanner);
}
